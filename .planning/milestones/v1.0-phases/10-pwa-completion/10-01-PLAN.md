---
phase: 10-pwa-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/icon-192.png
  - public/icon-512.png
  - src/app/api/generate-route/route.ts
autonomous: true
requirements:
  - PWA-01

must_haves:
  truths:
    - "icon-192.png and icon-512.png exist in public/ as valid PNG images"
    - "manifest.json icon references resolve to real files"
    - "Dead /api/generate-route directory no longer exists"
  artifacts:
    - path: "public/icon-192.png"
      provides: "192x192 PWA icon"
    - path: "public/icon-512.png"
      provides: "512x512 PWA icon"
  key_links:
    - from: "public/manifest.json"
      to: "public/icon-192.png"
      via: "icons[].src = /icon-192.png"
      pattern: "icon-192\\.png"
    - from: "public/manifest.json"
      to: "public/icon-512.png"
      via: "icons[].src = /icon-512.png"
      pattern: "icon-512\\.png"
---

<objective>
Generate PWA placeholder icons and remove dead server route.

Purpose: Complete PWA installability (browser won't show install prompt without valid icons) and clean up dead code.
Output: Two PNG icon files in public/, one deleted API directory.
</objective>

<execution_context>
@/Users/johannesstensson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johannesstensson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate PWA icons</name>
  <files>public/icon-192.png, public/icon-512.png</files>
  <action>
Generate two PNG icon files using a Node.js script (no external dependencies needed beyond what exists).

Create a temporary script that uses the `canvas` approach via inline SVG-to-PNG conversion:

1. Write an inline SVG string with a dark background (#0a0a0a) and a simple green (#22c55e) running figure or route/loop icon. Keep the design minimal -- a circle or loop shape with a runner silhouette works well. The design should look good at both 192px and 512px.

2. Use a Node.js script that:
   - Creates the SVG as a string
   - Uses `sharp` if available, OR writes a minimal valid PNG directly
   - Since this project may not have sharp/canvas installed, the simplest approach: use `npx sharp-cli` or write a self-contained script that converts SVG to PNG via the `resvg-js` package (npx compatible)

   Simplest reliable approach: install `sharp` as a dev dependency temporarily, run the conversion, then uninstall:
   ```
   npx --yes sharp-cli -i icon.svg -o public/icon-192.png -w 192 -h 192
   ```

   OR even simpler: use a data-URI approach with `fetch` to a free SVG-to-PNG API.

   The MOST reliable approach for a one-off: write a small Node script using `@resvg/resvg-js` via npx:
   ```js
   const { Resvg } = require('@resvg/resvg-js');
   const svg = `<svg>...</svg>`;
   const resvg192 = new Resvg(svg, { fitTo: { mode: 'width', value: 192 } });
   fs.writeFileSync('public/icon-192.png', resvg192.render().asPng());
   // same for 512
   ```

3. The SVG design: dark rounded-rect background (#0a0a0a), centered green (#22c55e) loop/circle icon suggesting a running route, with "RL" text or a simple running figure silhouette.

4. After generating, verify both files are valid PNGs (check file header bytes).

5. Confirm manifest.json already references `/icon-192.png` and `/icon-512.png` -- it does, no changes needed to manifest.json.

6. Clean up any temporary script files used for generation.
  </action>
  <verify>
    <automated>test -f public/icon-192.png && test -f public/icon-512.png && file public/icon-192.png | grep -q PNG && file public/icon-512.png | grep -q PNG && echo "PASS: Both icons are valid PNGs"</automated>
  </verify>
  <done>icon-192.png (192x192) and icon-512.png (512x512) exist in public/ as valid PNG files. manifest.json references match.</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead /api/generate-route directory</name>
  <files>src/app/api/generate-route/route.ts</files>
  <action>
Delete the entire `src/app/api/generate-route/` directory. This is dead code -- the app uses browser-direct Anthropic API calls (via route-ai.ts) instead of this server-side route.

1. Remove `src/app/api/generate-route/` directory and all contents.
2. Verify no other files import from or reference this API route path. Grep for "api/generate-route" across the codebase to confirm nothing depends on it.
3. If the `src/app/api/` directory is now empty, remove it too.
  </action>
  <verify>
    <automated>test ! -d src/app/api/generate-route && echo "PASS: Dead route removed"</automated>
  </verify>
  <done>The /api/generate-route/ directory no longer exists. No remaining references to it in the codebase.</done>
</task>

</tasks>

<verification>
- `ls public/icon-192.png public/icon-512.png` shows both files exist
- `file public/icon-*.png` confirms PNG format
- `cat public/manifest.json | grep icon` shows references match existing filenames
- `test ! -d src/app/api/generate-route` confirms dead code removed
</verification>

<success_criteria>
1. PWA install prompt can fire (valid icons referenced by manifest)
2. No dead server routes remain
3. `npm run build` still succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/10-pwa-completion/10-01-SUMMARY.md`
</output>
