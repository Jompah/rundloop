---
phase: 01-storage-gps-foundation
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - vitest.config.ts
  - package.json
  - src/lib/__tests__/gps-filter.test.ts
  - src/lib/__tests__/wake-lock.test.ts
  - src/lib/__tests__/db.test.ts
autonomous: true
requirements:
  - GPS-01
  - GPS-02
  - GPS-03
  - GPS-04
  - STOR-01
  - STOR-02
  - STOR-03
  - STOR-04

must_haves:
  truths:
    - "Running npx vitest run completes without configuration errors"
    - "Test stub files exist for all three modules (gps-filter, wake-lock, db) with placeholder test cases"
    - "fake-indexeddb is available for db tests to run against"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest configuration with path aliases matching tsconfig"
      contains: "defineConfig"
    - path: "src/lib/__tests__/gps-filter.test.ts"
      provides: "GPS filter test stubs for accuracy, jitter, teleport rejection"
      contains: "shouldAcceptPosition"
    - path: "src/lib/__tests__/wake-lock.test.ts"
      provides: "Wake Lock test stubs for acquire, release, visibility re-acquire"
      contains: "acquireWakeLock"
    - path: "src/lib/__tests__/db.test.ts"
      provides: "IndexedDB CRUD test stubs for runs, routes, settings, migration, persistence"
      contains: "dbPut"
  key_links:
    - from: "vitest.config.ts"
      to: "tsconfig.json"
      via: "path alias resolution matching @/ prefix"
      pattern: "resolve.*alias"
---

<objective>
Install test infrastructure (vitest + fake-indexeddb) and create test stub files so that all subsequent plans have automated verification commands available.

Purpose: Wave 0 test scaffolding is required by the Nyquist compliance rule — every plan task must have an automated verify command that can actually run. Without vitest installed and test files created, the `npx vitest run` commands in Plans 01-03 would fail.
Output: `vitest.config.ts`, three test stub files under `src/lib/__tests__/`, vitest and fake-indexeddb as dev dependencies.
</objective>

<execution_context>
@/Users/johannesstensson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johannesstensson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-storage-gps-foundation/01-CONTEXT.md
@.planning/phases/01-storage-gps-foundation/01-RESEARCH.md
@.planning/phases/01-storage-gps-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install vitest and fake-indexeddb, create vitest config</name>
  <files>package.json, vitest.config.ts</files>
  <read_first>
    - tsconfig.json (path aliases to replicate in vitest config)
    - package.json (current devDependencies)
  </read_first>
  <action>
    1. **Install dev dependencies:**
       ```bash
       npm install -D vitest fake-indexeddb
       ```

    2. **Create `vitest.config.ts`** at project root:
       ```typescript
       import { defineConfig } from 'vitest/config';
       import path from 'path';

       export default defineConfig({
         test: {
           environment: 'node',
           globals: true,
           include: ['src/**/__tests__/**/*.test.ts'],
         },
         resolve: {
           alias: {
             '@': path.resolve(__dirname, './src'),
           },
         },
       });
       ```
       - The `@` alias must match what's in `tsconfig.json` paths
       - `globals: true` enables `describe`, `it`, `expect` without imports
       - No watch mode flags — test runner exits after completion
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <done>vitest and fake-indexeddb are installed as dev dependencies. vitest.config.ts exists with path aliases matching tsconfig. Running `npx vitest run` completes without configuration errors (may show 0 tests if stubs aren't created yet).</done>
</task>

<task type="auto">
  <name>Task 2: Create test stub files for gps-filter, wake-lock, and db</name>
  <files>src/lib/__tests__/gps-filter.test.ts, src/lib/__tests__/wake-lock.test.ts, src/lib/__tests__/db.test.ts</files>
  <action>
    Create the `src/lib/__tests__/` directory and three test stub files. Each file contains `describe` blocks with `it.todo()` placeholders for the test cases described in VALIDATION.md. These are stubs — they define WHAT to test but do not implement assertions yet. The implementation plans (01-01, 01-02, 01-03) will fill in the test bodies as part of their TDD workflow.

    **`src/lib/__tests__/gps-filter.test.ts`** — GPS filter unit tests (GPS-01, GPS-02):
    ```typescript
    import { describe, it, expect } from 'vitest';

    describe('shouldAcceptPosition', () => {
      it.todo('accepts first position when accuracy is within threshold (<=30m)');
      it.todo('rejects position with accuracy > 30m');
      it.todo('rejects position less than 3m from previous accepted (jitter)');
      it.todo('rejects position implying speed > 45 km/h (teleport)');
      it.todo('accepts position with valid accuracy, distance, and speed');
      it.todo('accepts position when time delta is 0 (avoids division by zero)');
    });

    describe('watchFilteredPosition', () => {
      it.todo('calls onAccepted for valid positions');
      it.todo('calls onRejected with reason for invalid positions');
      it.todo('maintains stateful last-accepted tracking across calls');
    });
    ```

    **`src/lib/__tests__/wake-lock.test.ts`** — Wake Lock manager tests (GPS-03):
    ```typescript
    import { describe, it, expect } from 'vitest';

    describe('acquireWakeLock', () => {
      it.todo('returns true when wake lock is acquired successfully');
      it.todo('returns false when Wake Lock API is not supported');
      it.todo('returns false when request throws an error');
    });

    describe('releaseWakeLock', () => {
      it.todo('releases an active wake lock');
      it.todo('handles release when no wake lock is active');
    });

    describe('setupVisibilityReacquire', () => {
      it.todo('re-acquires wake lock when page becomes visible');
      it.todo('returns cleanup function that removes event listener');
    });

    describe('isWakeLockSupported', () => {
      it.todo('returns true when navigator.wakeLock exists');
      it.todo('returns false in SSR environment');
    });
    ```

    **`src/lib/__tests__/db.test.ts`** — IndexedDB CRUD and related tests (GPS-04, STOR-01..04):
    ```typescript
    import { describe, it, expect } from 'vitest';

    describe('IndexedDB CRUD', () => {
      it.todo('dbPut stores a record and dbGet retrieves it');
      it.todo('dbPut overwrites existing record with same key');
      it.todo('dbDelete removes a record');
      it.todo('dbGetAll returns all records from a store');
    });

    describe('runs store (STOR-01, GPS-04)', () => {
      it.todo('stores a completed run with full GPS trace');
      it.todo('stores an active run snapshot (crash recovery)');
      it.todo('snapshot overwrites previous snapshot for same run ID');
      it.todo('findIncompleteRun returns ActiveRunSnapshot without endTime');
      it.todo('findIncompleteRun returns null when no incomplete runs exist');
      it.todo('clearIncompleteRun removes the incomplete run record');
    });

    describe('routes store (STOR-02)', () => {
      it.todo('stores and retrieves a saved route');
      it.todo('deletes a saved route by ID');
    });

    describe('settings store (STOR-04)', () => {
      it.todo('stores and retrieves app settings');
      it.todo('merges with default settings when fields are missing');
    });

    describe('migration (STOR-01..04)', () => {
      it.todo('migrates settings from localStorage to IndexedDB');
      it.todo('migrates saved routes from localStorage to IndexedDB');
      it.todo('skips migration if already migrated');
      it.todo('handles corrupt localStorage data gracefully');
    });

    describe('storage persistence (STOR-03)', () => {
      it.todo('requests persistent storage via navigator.storage.persist()');
      it.todo('returns false when API is not available');
      it.todo('returns true when already persisted');
    });
    ```

    All test files use `it.todo()` which vitest reports as "skipped" — they won't fail the test run but clearly show what needs implementation.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Three test stub files exist under src/lib/__tests__/ with todo test cases covering all phase requirements. Running `npx vitest run` shows the stubs as skipped/todo without failures.</done>
</task>

</tasks>

<verification>
- `npx vitest run` completes without errors
- `vitest.config.ts` exists with @ alias matching tsconfig
- `src/lib/__tests__/gps-filter.test.ts` contains shouldAcceptPosition and watchFilteredPosition describe blocks
- `src/lib/__tests__/wake-lock.test.ts` contains acquireWakeLock, releaseWakeLock, setupVisibilityReacquire describe blocks
- `src/lib/__tests__/db.test.ts` contains IndexedDB CRUD, runs, routes, settings, migration, persistence describe blocks
- vitest and fake-indexeddb appear in package.json devDependencies
</verification>

<success_criteria>
- `npx vitest run` exits 0 with all test stubs reported as todo/skipped
- Test file paths match the automated commands referenced in Plans 01-03
- vitest.config.ts resolves the @ path alias correctly
</success_criteria>

<output>
After completion, create `.planning/phases/01-storage-gps-foundation/01-00-SUMMARY.md`
</output>
