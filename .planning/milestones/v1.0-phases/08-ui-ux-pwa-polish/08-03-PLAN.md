---
phase: 08-ui-ux-pwa-polish
plan: 03
type: execute
wave: 3
depends_on: [08-01, 08-02]
files_modified:
  - src/components/EndRunDialog.tsx
  - src/components/CrashRecoveryDialog.tsx
  - src/components/DiscardConfirmDialog.tsx
  - src/components/DeleteRunDialog.tsx
  - src/components/DeleteRouteDialog.tsx
  - src/components/NavigationView.tsx
  - src/components/RouteGenerator.tsx
  - src/components/RunSummaryView.tsx
  - src/components/SettingsView.tsx
  - src/components/RunHistoryView.tsx
  - src/components/SavedRoutesView.tsx
  - src/components/TabBar.tsx
  - src/app/page.tsx
autonomous: true
requirements: [UI-01, UI-04, UI-05]

must_haves:
  truths:
    - "All buttons across all components use the shared Button component instead of inline styles"
    - "All touch targets are at least 44px tall"
    - "No horizontal overflow at 375px viewport width"
    - "Haptic feedback fires on start run, pause, resume, end run, and milestone announcement"
    - "Typography is consistent: same font sizes and weights used for same-level headings across screens"
    - "Spacing follows 8pt grid (p-2=8px, p-3=12px, p-4=16px, p-6=24px)"
  artifacts:
    - path: "src/components/EndRunDialog.tsx"
      provides: "Dialog using Button component"
      contains: "Button"
    - path: "src/components/NavigationView.tsx"
      provides: "Navigation with haptic-wired controls"
      contains: "haptic"
    - path: "src/app/page.tsx"
      provides: "Start Run button using Button + haptic"
      contains: "haptic"
  key_links:
    - from: "src/components/NavigationView.tsx"
      to: "src/lib/haptics.ts"
      via: "import { haptic }"
      pattern: "haptic\\("
    - from: "src/app/page.tsx"
      to: "src/lib/haptics.ts"
      via: "import { haptic }"
      pattern: "haptic\\("
    - from: "src/components/EndRunDialog.tsx"
      to: "src/components/ui/Button.tsx"
      via: "import { Button }"
      pattern: "<Button"
---

<objective>
Roll out the shared Button component across all dialog and view components, wire haptic feedback into run control actions, and audit all screens for 375px viewport compliance and consistent typography/spacing.

Purpose: Achieve design system consistency (UI-01), mobile viewport optimization (UI-04), and premium minimal interface (UI-05) by replacing ad-hoc button styles with the shared component and adding haptic feedback to key moments.
Output: All components using Button, haptics wired, viewport-safe layouts
</objective>

<execution_context>
@/Users/johannesstensson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johannesstensson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-ux-pwa-polish/08-RESEARCH.md
@.planning/phases/08-ui-ux-pwa-polish/08-01-SUMMARY.md
@.planning/phases/08-ui-ux-pwa-polish/08-02-SUMMARY.md

@src/app/page.tsx
@src/components/EndRunDialog.tsx
@src/components/NavigationView.tsx

<interfaces>
From src/components/ui/Button.tsx (created in Plan 01):
```typescript
interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: 'primary' | 'secondary' | 'destructive';
  size?: 'sm' | 'md' | 'lg';
  fullWidth?: boolean;
}
export function Button(props: ButtonProps): JSX.Element;
```

From src/lib/haptics.ts (created in Plan 01):
```typescript
export type HapticPattern = 'tap' | 'success' | 'milestone' | 'warning';
export function haptic(pattern: HapticPattern): void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Roll out Button component across all dialogs and wire haptics into run controls</name>
  <files>src/components/EndRunDialog.tsx, src/components/CrashRecoveryDialog.tsx, src/components/DiscardConfirmDialog.tsx, src/components/DeleteRunDialog.tsx, src/components/DeleteRouteDialog.tsx, src/components/NavigationView.tsx, src/app/page.tsx</files>
  <read_first>src/components/EndRunDialog.tsx, src/components/CrashRecoveryDialog.tsx, src/components/DiscardConfirmDialog.tsx, src/components/DeleteRunDialog.tsx, src/components/DeleteRouteDialog.tsx, src/components/NavigationView.tsx, src/app/page.tsx</read_first>
  <action>
    **Button rollout in dialogs:**

    For each dialog component, replace inline-styled button elements with the Button component. The dialog wrappers already use motion.div from Plan 02 — only replace the action buttons inside.

    1. EndRunDialog.tsx:
       - Import { Button } from '@/components/ui/Button'
       - "Keep Going" button -> `<Button variant="secondary" fullWidth onClick={onCancel}>Keep Going</Button>`
       - "End Run" button -> `<Button variant="destructive" fullWidth onClick={onConfirm}>End Run</Button>`
       - Remove the flex-1 from buttons, use a flex gap-3 container with two fullWidth buttons

    2. CrashRecoveryDialog.tsx — same pattern:
       - "Discard" -> Button variant="secondary"
       - "Resume Run" -> Button variant="primary"

    3. DiscardConfirmDialog.tsx:
       - "Keep" -> Button variant="secondary"
       - "Discard" -> Button variant="destructive"

    4. DeleteRunDialog.tsx:
       - "Cancel" -> Button variant="secondary"
       - "Delete" -> Button variant="destructive"

    5. DeleteRouteDialog.tsx:
       - "Cancel" -> Button variant="secondary"
       - "Delete" -> Button variant="destructive"

    **Haptic wiring:**

    6. In NavigationView.tsx:
       - Import { haptic } from '@/lib/haptics'
       - Add haptic('tap') call inside the onPause handler (before calling props.onPause)
       - Add haptic('tap') call inside the onResume handler
       - Add haptic('success') call inside the onEndRun handler
       - Add haptic('milestone') in the milestone detection effect where speak() is called

    7. In page.tsx:
       - Import { haptic } from '@/lib/haptics'
       - Add haptic('success') before runSession.startRun() in the "Start Run" onClick
       - Replace the inline Start Run button with `<Button variant="primary" size="lg" onClick={...}>Start Run</Button>`
       - Replace the "Ny rutt" button with `<Button variant="secondary" fullWidth onClick={...}>` with the X icon and text inside
  </action>
  <verify>
    <automated>cd /Users/johannesstensson/rundloop && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "import { Button }" src/components/EndRunDialog.tsx
    - grep -q "import { Button }" src/components/CrashRecoveryDialog.tsx
    - grep -q "import { Button }" src/components/DiscardConfirmDialog.tsx
    - grep -q "import { Button }" src/components/DeleteRunDialog.tsx
    - grep -q "import { Button }" src/components/DeleteRouteDialog.tsx
    - grep -q "import { haptic }" src/components/NavigationView.tsx
    - grep -q "import { haptic }" src/app/page.tsx
    - grep -q "haptic(" src/components/NavigationView.tsx
    - grep -q "haptic(" src/app/page.tsx
    - grep -q "<Button" src/components/EndRunDialog.tsx
  </acceptance_criteria>
  <done>All 5 dialogs use Button component. Haptic feedback fires on start/pause/resume/end run and milestone. Start Run in page.tsx uses Button component.</done>
</task>

<task type="auto">
  <name>Task 2: Button rollout in views, typography audit, and viewport 375px compliance</name>
  <files>src/components/RouteGenerator.tsx, src/components/RunSummaryView.tsx, src/components/SettingsView.tsx, src/components/RunHistoryView.tsx, src/components/SavedRoutesView.tsx, src/components/TabBar.tsx</files>
  <read_first>src/components/RouteGenerator.tsx, src/components/RunSummaryView.tsx, src/components/SettingsView.tsx, src/components/RunHistoryView.tsx, src/components/SavedRoutesView.tsx, src/components/TabBar.tsx</read_first>
  <action>
    **Button rollout in view components:**

    1. RouteGenerator.tsx:
       - Import { Button } from '@/components/ui/Button'
       - Replace "Generate Route" / "Save Route" button with Button variant="primary"
       - Replace any secondary action buttons with Button variant="secondary"
       - Ensure all buttons have min-h-[44px] via Button component

    2. RunSummaryView.tsx:
       - Replace "Save Run" button with Button variant="primary" fullWidth
       - Replace "Discard" button with Button variant="secondary" fullWidth

    3. SettingsView.tsx:
       - Replace "Done" / close button with Button variant="primary"
       - Ensure voice style pills maintain 44px touch targets (add min-h-[44px] if needed)

    4. RunHistoryView.tsx:
       - Ensure card buttons have 44px min touch targets
       - No button replacement needed if cards use button elements already

    5. SavedRoutesView.tsx:
       - Replace "Run" action buttons with Button variant="primary" size="sm"
       - Replace "Delete" buttons with Button variant="destructive" size="sm"

    **Typography audit (apply across ALL modified components):**
    - Screen titles: text-xl font-bold text-white (consistent)
    - Section labels: text-sm font-medium text-gray-400
    - Body text: text-base text-white
    - Stats/numbers: text-2xl font-bold text-white (for distances, times)
    - Fix any inconsistencies found during the audit

    **Viewport 375px compliance:**
    - TabBar.tsx: Ensure icons + labels fit at 375px, no overflow. Add max-w-full if needed.
    - RouteGenerator.tsx: Check distance slider and buttons don't overflow. Add overflow-hidden to containers.
    - All views: Ensure no fixed-width elements exceed 375px. Use w-full or max-w-full patterns.
    - All padded containers: Use px-4 (16px each side = 32px total, leaving 343px content on 375px screen)
    - Check for any hardcoded width values that could cause overflow

    **Spacing audit (8pt grid):**
    - Ensure gap values are multiples of 8px: gap-1(4px) OK for small, gap-2(8px), gap-3(12px), gap-4(16px), gap-6(24px)
    - Replace any odd spacing values (gap-5=20px -> gap-4 or gap-6)
  </action>
  <verify>
    <automated>cd /Users/johannesstensson/rundloop && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "import { Button }" src/components/RouteGenerator.tsx
    - grep -q "import { Button }" src/components/RunSummaryView.tsx
    - grep -q "<Button" src/components/RouteGenerator.tsx
    - grep -q "<Button" src/components/RunSummaryView.tsx
    - grep -rL "min-h-\[44px\]\|Button" src/components/SavedRoutesView.tsx | wc -l | grep -q "0"
  </acceptance_criteria>
  <done>All view components use Button for actions. Typography consistent across screens. No horizontal overflow at 375px. Touch targets >= 44px. Spacing follows 8pt grid.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` compiles without errors
- `npx vitest run --reporter=verbose` all tests pass
- Every dialog and view component imports and uses Button
- haptic() called in NavigationView for pause/resume/end/milestone
- haptic() called in page.tsx for start run
- No hardcoded widths that could overflow 375px viewport
</verification>

<success_criteria>
Shared Button component used consistently across all ~15 components. Haptic feedback fires on 6 key interactions (start, pause, resume, end, milestone, off-route alert). All screens pass 375px viewport audit with no overflow. Typography scale is consistent. Design system is cohesive.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-ux-pwa-polish/08-03-SUMMARY.md`
</output>
