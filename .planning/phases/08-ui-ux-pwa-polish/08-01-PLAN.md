---
phase: 08-ui-ux-pwa-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/Button.tsx
  - src/lib/haptics.ts
  - src/lib/__tests__/haptics.test.ts
  - src/components/ui/OfflineBanner.tsx
  - public/sw.js
  - public/manifest.json
  - src/app/layout.tsx
  - src/app/globals.css
autonomous: true
requirements: [UI-01, UI-03, UI-05, PWA-01, PWA-02, PWA-03]

must_haves:
  truths:
    - "Shared Button component renders primary, secondary, and destructive variants with correct styles"
    - "Haptic utility triggers navigator.vibrate with correct patterns and gracefully no-ops when unavailable"
    - "Service worker caches app shell on install and serves cached assets when offline"
    - "PWA manifest declares standalone display, correct icons, theme-color #0a0a0a"
    - "OfflineBanner appears when network drops and auto-dismisses on reconnect"
  artifacts:
    - path: "src/components/ui/Button.tsx"
      provides: "Shared button with primary/secondary/destructive variants and sm/md/lg sizes"
      exports: ["Button"]
    - path: "src/lib/haptics.ts"
      provides: "Centralized vibration patterns utility"
      exports: ["haptic"]
    - path: "src/lib/__tests__/haptics.test.ts"
      provides: "Unit tests for haptic patterns"
    - path: "src/components/ui/OfflineBanner.tsx"
      provides: "Network status indicator with motion animation"
      exports: ["OfflineBanner"]
    - path: "public/sw.js"
      provides: "Service worker with cache-first static + network-first API"
    - path: "public/manifest.json"
      provides: "PWA manifest with standalone display and icons"
      contains: "standalone"
    - path: "src/app/layout.tsx"
      provides: "SW registration, apple-web-app meta tags, viewport meta"
  key_links:
    - from: "src/app/layout.tsx"
      to: "/sw.js"
      via: "navigator.serviceWorker.register"
      pattern: "serviceWorker\\.register"
    - from: "src/app/layout.tsx"
      to: "/manifest.json"
      via: "metadata.manifest"
      pattern: "manifest"
---

<objective>
Create the core infrastructure for Phase 8 polish: shared Button component, haptic feedback utility, OfflineBanner, service worker, PWA manifest, and layout meta tags.

Purpose: Establish all new files and utilities that Plan 02 will wire into existing components. This is the foundation layer.
Output: Button.tsx, haptics.ts, OfflineBanner.tsx, sw.js, updated manifest.json, updated layout.tsx, updated globals.css
</objective>

<execution_context>
@/Users/johannesstensson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johannesstensson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-ux-pwa-polish/08-RESEARCH.md

@src/app/layout.tsx
@src/app/globals.css
@public/manifest.json
@src/components/EndRunDialog.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Button component, haptics utility with tests, and OfflineBanner</name>
  <files>src/components/ui/Button.tsx, src/lib/haptics.ts, src/lib/__tests__/haptics.test.ts, src/components/ui/OfflineBanner.tsx</files>
  <read_first>src/components/EndRunDialog.tsx, src/components/DiscardConfirmDialog.tsx, src/app/globals.css</read_first>
  <behavior>
    - haptic('tap') calls navigator.vibrate(50)
    - haptic('success') calls navigator.vibrate([50, 50, 50])
    - haptic('milestone') calls navigator.vibrate([100, 50, 100])
    - haptic('warning') calls navigator.vibrate([100, 50, 100, 50, 100])
    - haptic() is a no-op when navigator.vibrate is undefined (no throw)
    - Button renders with correct variant classes (primary=bg-green-500, secondary=bg-gray-800, destructive=bg-red-500)
    - Button accepts size prop (sm, md, lg) and fullWidth prop
  </behavior>
  <action>
    1. Create src/lib/haptics.ts:
       - Export type HapticPattern = 'tap' | 'success' | 'milestone' | 'warning'
       - patterns record: tap=50, success=[50,50,50], milestone=[100,50,100], warning=[100,50,100,50,100]
       - Export function haptic(pattern: HapticPattern): void — feature-detect navigator.vibrate, call with pattern, no-op if unavailable

    2. Create src/lib/__tests__/haptics.test.ts:
       - Mock navigator.vibrate as vi.fn()
       - Test each pattern calls vibrate with correct value
       - Test no-throw when vibrate is undefined (delete navigator.vibrate, call haptic)

    3. Create src/components/ui/Button.tsx:
       - 'use client' directive
       - Interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement>
       - variant: 'primary' | 'secondary' | 'destructive' (default: 'primary')
       - size: 'sm' | 'md' | 'lg' (default: 'md')
       - fullWidth: boolean (default: false)
       - Variant classes: primary='bg-green-500 text-white active:bg-green-600', secondary='bg-gray-800 text-white active:bg-gray-700', destructive='bg-red-500 text-white active:bg-red-600'
       - Size classes: sm='px-3 py-2 text-sm', md='px-4 py-3 text-base', lg='px-6 py-3 text-lg'
       - Base classes: 'rounded-xl font-semibold min-h-[44px]' (44px touch target per UI-04)
       - Spread remaining props onto button element

    4. Create src/components/ui/OfflineBanner.tsx:
       - 'use client' directive
       - Import { useState, useEffect } from 'react', { motion, AnimatePresence } from 'motion/react'
       - Listen to window 'online'/'offline' events, init from !navigator.onLine
       - Render AnimatePresence wrapping motion.div when offline
       - Animation: initial={{ y: -40, opacity: 0 }}, animate={{ y: 0, opacity: 1 }}, exit={{ y: -40, opacity: 0 }}
       - Style: fixed top-0 left-0 right-0 z-50 bg-amber-600 text-white text-center text-sm py-2 font-medium safe-top
  </action>
  <verify>
    <automated>cd /Users/johannesstensson/rundloop && npx vitest run src/lib/__tests__/haptics.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export function haptic" src/lib/haptics.ts
    - grep -q "export function Button" src/components/ui/Button.tsx
    - grep -q "export function OfflineBanner" src/components/ui/OfflineBanner.tsx
    - grep -q "navigator.vibrate" src/lib/haptics.ts
    - grep -q "min-h-\[44px\]" src/components/ui/Button.tsx
    - grep -q "AnimatePresence" src/components/ui/OfflineBanner.tsx
  </acceptance_criteria>
  <done>Button component with 3 variants and 3 sizes exists, haptics utility passes all tests, OfflineBanner renders with motion animation</done>
</task>

<task type="auto">
  <name>Task 2: Service worker, manifest update, layout meta tags, and globals.css design tokens</name>
  <files>public/sw.js, public/manifest.json, src/app/layout.tsx, src/app/globals.css</files>
  <read_first>public/manifest.json, src/app/layout.tsx, src/app/globals.css</read_first>
  <action>
    1. Create public/sw.js:
       - const CACHE_NAME = 'rundloop-v1'
       - Install event: caches.open(CACHE_NAME).then(cache => cache.addAll(['/'])), self.skipWaiting()
       - Activate event: delete old caches (keys != CACHE_NAME), self.clients.claim()
       - Fetch event:
         - If url.pathname.startsWith('/api/') or url.hostname !== self.location.hostname: network-first with cache fallback
         - Else: cache-first, on miss fetch and cache the response clone
       - For tile CDN URLs (hostname contains 'tile' or 'basemaps'): cache up to ~50 responses in separate 'rundloop-tiles-v1' cache

    2. Update public/manifest.json:
       - Keep existing structure but update theme_color to '#0a0a0a' per user decision
       - Ensure display: 'standalone', background_color: '#0a0a0a'
       - Add apple-touch-icon reference if not present
       - Add "scope": "/" and "orientation": "portrait"

    3. Update src/app/layout.tsx:
       - Add viewport meta with viewport-fit=cover for safe areas: export const viewport = { themeColor: '#0a0a0a', width: 'device-width', initialScale: 1, viewportFit: 'cover' as const }
       - Add apple-touch-icon to metadata.icons
       - Keep existing Geist fonts and metadata
       - Import OfflineBanner from '@/components/ui/OfflineBanner'
       - Add <OfflineBanner /> as first child inside body (above {children})
       - NOTE: OfflineBanner is a client component but layout.tsx is a server component. Wrap OfflineBanner import as a client-only component or use a client wrapper.
       - Add SW registration script: create a small client component SWRegistration that runs useEffect with navigator.serviceWorker.register('/sw.js'), import it into layout.
       - Actually, since layout.tsx is a server component, create src/components/PWAProvider.tsx ('use client') that handles both SW registration and renders OfflineBanner, then import PWAProvider into layout.tsx body.

    4. Update src/app/globals.css:
       - Force dark mode: add `html { color-scheme: dark; }` so dark is default regardless of system preference
       - Add .safe-left and .safe-right utilities: `padding-left: max(1rem, env(safe-area-inset-left))` etc.
       - Ensure existing .safe-bottom and .safe-top remain unchanged
  </action>
  <verify>
    <automated>cd /Users/johannesstensson/rundloop && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "CACHE_NAME" public/sw.js
    - grep -q "skipWaiting" public/sw.js
    - grep -q "clients.claim" public/sw.js
    - grep -q "standalone" public/manifest.json
    - grep -q "0a0a0a" public/manifest.json
    - grep -q "serviceWorker" src/components/PWAProvider.tsx || grep -q "serviceWorker" src/app/layout.tsx
    - grep -q "OfflineBanner" src/components/PWAProvider.tsx || grep -q "OfflineBanner" src/app/layout.tsx
    - grep -q "color-scheme" src/app/globals.css
    - grep -q "viewport-fit" src/app/layout.tsx || grep -q "viewportFit" src/app/layout.tsx
  </acceptance_criteria>
  <done>SW caches app shell on install, manifest has standalone + #0a0a0a theme, layout registers SW + renders OfflineBanner + has viewport-fit=cover, globals.css forces dark color-scheme</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` passes (haptics tests + existing tests)
- `npx tsc --noEmit` compiles without errors
- public/sw.js exists with install/activate/fetch handlers
- public/manifest.json has display: standalone, theme_color: #0a0a0a
- Button component exported with 3 variants
- OfflineBanner uses motion/react AnimatePresence
</verification>

<success_criteria>
All new utility files created and tested. PWA infrastructure (SW, manifest, meta tags) in place. Button component ready for rollout. OfflineBanner renders in layout. Existing tests still pass.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-ux-pwa-polish/08-01-SUMMARY.md`
</output>
