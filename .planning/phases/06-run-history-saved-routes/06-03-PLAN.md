---
phase: 06-run-history-saved-routes
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/components/SavedRoutesView.tsx
  - src/components/DeleteRouteDialog.tsx
autonomous: true
requirements: [ROUT-02, ROUT-03, ROUT-04]

must_haves:
  truths:
    - "Saved routes list shows all saved routes with name, distance, and thumbnail"
    - "User can tap 'Run' to load a saved route and start navigation"
    - "User can rename a saved route inline with tap-to-edit"
    - "User can delete a saved route via confirmation dialog"
    - "Empty state shows 'No saved routes' message"
  artifacts:
    - path: "src/components/SavedRoutesView.tsx"
      provides: "Saved routes list with card layout, inline rename, run and delete actions"
      exports: ["SavedRoutesView"]
      min_lines: 80
    - path: "src/components/DeleteRouteDialog.tsx"
      provides: "Confirmation dialog for route deletion"
      exports: ["DeleteRouteDialog"]
  key_links:
    - from: "src/components/SavedRoutesView.tsx"
      to: "src/lib/storage.ts"
      via: "getSavedRoutes, deleteRoute, dbPut for rename"
      pattern: "getSavedRoutes|deleteRoute"
    - from: "src/components/SavedRoutesView.tsx"
      to: "src/components/RouteThumbnail.tsx"
      via: "RouteThumbnail import for card thumbnails"
      pattern: "RouteThumbnail"
---

<objective>
Build the saved routes feature: a scrollable card list of saved routes with inline rename, "Run" button to load for navigation, and delete capability.

Purpose: Runners can browse, rename, reuse, and manage their saved routes (ROUT-02 through ROUT-04).
Output: Two new components implementing saved route browsing and management.
</objective>

<execution_context>
@/Users/johannesstensson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johannesstensson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-run-history-saved-routes/06-CONTEXT.md
@.planning/phases/06-run-history-saved-routes/06-RESEARCH.md
@.planning/phases/06-run-history-saved-routes/06-UI-SPEC.md
@.planning/phases/06-run-history-saved-routes/06-01-SUMMARY.md

<interfaces>
<!-- Types and utilities available from prior plans and codebase -->

From src/lib/storage.ts (after Plan 01):
```typescript
export interface SavedRoute {
  id: string;
  name?: string;
  route: GeneratedRoute;
  city: string;
  createdAt: string;
}

export async function getSavedRoutes(): Promise<SavedRoute[]>;
export async function saveRoute(route: GeneratedRoute, city: string, name?: string): Promise<SavedRoute>;
export async function deleteRoute(id: string): Promise<void>;
```

From src/lib/db.ts:
```typescript
export async function dbPut<T>(storeName: string, value: T): Promise<void>;
```

From src/types/index.ts:
```typescript
export interface GeneratedRoute {
  waypoints: RouteWaypoint[];
  polyline: [number, number][]; // [lng, lat] pairs
  distance: number; // meters
  duration: number; // seconds
  instructions: TurnInstruction[];
}
```

From src/components/RouteThumbnail.tsx (Plan 01):
```typescript
export function RouteThumbnail(props: { points: { lat: number; lng: number }[], size?: number, color?: string }): JSX.Element;
```

From src/components/DiscardConfirmDialog.tsx (pattern reference):
```typescript
// ~31 lines, fixed inset-0 z-50 bg-black/60 backdrop, bg-gray-900 rounded-2xl p-6 card
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DeleteRouteDialog</name>
  <files>src/components/DeleteRouteDialog.tsx</files>
  <read_first>src/components/DiscardConfirmDialog.tsx</read_first>
  <action>
Create src/components/DeleteRouteDialog.tsx — clone DiscardConfirmDialog pattern exactly:

- Props: `onConfirm: () => void`, `onCancel: () => void`
- Backdrop: `fixed inset-0 z-50 bg-black/60 flex items-center justify-center`
- Card: `bg-gray-900 rounded-2xl p-6 mx-4 max-w-sm w-full`
- Title: "Delete Route?" — `text-xl font-bold text-white`
- Body: "This saved route will be permanently removed." — `text-gray-400 mt-2`
- Button row: `flex gap-3 mt-6`
- Cancel: `flex-1 py-3 rounded-xl font-semibold bg-gray-800 text-white` — "Keep It"
- Confirm: `flex-1 py-3 rounded-xl font-semibold bg-red-500 text-white` — "Delete"
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
- grep -q "Delete Route?" src/components/DeleteRouteDialog.tsx returns 0
- grep -q "Keep It" src/components/DeleteRouteDialog.tsx returns 0
- TypeScript compiles without errors
  </acceptance_criteria>
  <done>DeleteRouteDialog renders confirmation dialog matching DiscardConfirmDialog pattern with "Delete Route?" title.</done>
</task>

<task type="auto">
  <name>Task 2: Create SavedRoutesView with cards, inline rename, run and delete actions</name>
  <files>src/components/SavedRoutesView.tsx</files>
  <read_first>src/lib/storage.ts, src/components/RouteThumbnail.tsx</read_first>
  <action>
Create src/components/SavedRoutesView.tsx:

Props:
- `onRunRoute: (route: GeneratedRoute) => void` — callback when user taps "Run" on a saved route
- No other props needed — data loaded internally

State:
- `routes: SavedRoute[]` — loaded from IndexedDB
- `loading: boolean`
- `editingId: string | null` — ID of route currently being renamed
- `editName: string` — current text in the rename input
- `deleteTarget: SavedRoute | null` — route pending deletion (shows DeleteRouteDialog)

Data loading (useEffect on mount):
- Call `getSavedRoutes()` from storage.ts
- Sort by createdAt descending (newest first)
- Set routes and loading=false

Layout:
- Full-height scrollable container: `flex flex-col h-full overflow-y-auto pb-16`
- Header: `px-4 pt-4 pb-2`, heading "Routes" in `text-xl font-bold text-white`

Empty state (routes.length === 0 and not loading):
- Centered: `flex flex-col items-center justify-center flex-1`
- Icon: 48x48 route/path SVG in `text-gray-600`
- Heading: "No saved routes" — `text-lg font-semibold text-gray-400 mt-4`
- Body: "Generate a route and tap Save to add it here" — `text-sm text-gray-500 mt-2 text-center max-w-xs`

Card list — each SavedRouteCard:
- Container: `mx-4 mb-3 bg-gray-800 rounded-2xl p-4 flex items-center gap-4`
- Left: `<RouteThumbnail points={route.route.polyline.map(([lng, lat]) => ({ lat, lng }))} size={80} />`
  Note: polyline is [lng, lat] but RouteThumbnail expects {lat, lng} — map to correct format
- Center (flex-1):
  - Name area: if `editingId === route.id`:
    - Show `<input>` with classes `bg-gray-700 text-white rounded-lg px-2 py-1 text-base w-full outline-none focus:ring-1 focus:ring-green-400`
    - Value: editName, onChange updates editName
    - autoFocus, onBlur: save, onKeyDown: Enter saves, Escape cancels
    - Save logic: if editName.trim() is empty, revert to auto-generated name (`${(route.route.distance/1000).toFixed(1)} km route - ${new Date(route.createdAt).toLocaleDateString('en', {month:'short', day:'numeric'})}`). Call `dbPut('routes', {...route, name: finalName})`. Update local state. Set editingId=null.
  - Else: show name as `<span>` with `text-base font-semibold text-white cursor-pointer`, onClick sets editingId and editName
  - Display name: `route.name || \`${(route.route.distance/1000).toFixed(1)} km route - ${new Date(route.createdAt).toLocaleDateString('en', {month:'short', day:'numeric'})}\``
  - Distance: `text-sm text-gray-400` — `${(route.route.distance / 1000).toFixed(1)} km`
  - Delete button: small `text-red-400 text-xs mt-1 cursor-pointer` — "Delete" text, onClick sets deleteTarget
- Right: "Run" button — `bg-green-500 text-white rounded-xl px-4 py-2 font-semibold shrink-0`
  - onClick: `onRunRoute(route.route)`

DeleteRouteDialog (when deleteTarget is set):
- onConfirm: call `deleteRoute(deleteTarget.id)`, remove from local routes state, set deleteTarget=null
- onCancel: set deleteTarget=null

Import DeleteRouteDialog from ./DeleteRouteDialog.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
- grep -q "SavedRoutesView" src/components/SavedRoutesView.tsx returns 0
- grep -q "getSavedRoutes" src/components/SavedRoutesView.tsx returns 0
- grep -q "onRunRoute" src/components/SavedRoutesView.tsx returns 0
- grep -q "editingId" src/components/SavedRoutesView.tsx returns 0
- grep -q "No saved routes" src/components/SavedRoutesView.tsx returns 0
- grep -q "RouteThumbnail" src/components/SavedRoutesView.tsx returns 0
- grep -q "DeleteRouteDialog" src/components/SavedRoutesView.tsx returns 0
- TypeScript compiles without errors
  </acceptance_criteria>
  <done>SavedRoutesView displays saved routes as cards with name, distance, thumbnail. Inline name editing works with tap-to-edit, blur/Enter to save, Escape to cancel. "Run" button triggers onRunRoute callback. Delete flow uses DeleteRouteDialog confirmation.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- SavedRoutesView loads routes from IndexedDB and renders card list
- Inline rename edits the name field and persists to IndexedDB
- "Run" button calls onRunRoute with the route's GeneratedRoute
- Delete flow shows confirmation then removes from IndexedDB and list
</verification>

<success_criteria>
- Saved routes view lists all saved routes with name, distance, and canvas thumbnail
- "Run" button on each card triggers navigation callback
- Inline rename allows editing route names with keyboard controls
- Delete confirmation dialog follows established pattern
- Empty state displayed when no saved routes exist
</success_criteria>

<output>
After completion, create `.planning/phases/06-run-history-saved-routes/06-03-SUMMARY.md`
</output>
