---
phase: 07-route-visualization
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/components/RunSummaryView.tsx
  - src/components/RunDetailOverlay.tsx
autonomous: true
requirements:
  - VIZ-01
  - VIZ-02
  - VIZ-03
  - VIZ-05
must_haves:
  truths:
    - "RunSummaryView shows the planned route with elevation gradient coloring"
    - "RunDetailOverlay shows the planned route with elevation gradient coloring"
    - "Both views show start/finish markers on the planned route"
    - "Both views use smooth anti-aliased lines (round caps/joins)"
    - "Route line widths follow locked decisions: 3px for secondary route rendering"
  artifacts:
    - path: "src/components/RunSummaryView.tsx"
      provides: "Post-run summary with gradient route overlay"
      contains: "addGradientRoute"
    - path: "src/components/RunDetailOverlay.tsx"
      provides: "Run detail view with gradient route overlay"
      contains: "addGradientRoute"
  key_links:
    - from: "src/components/RunSummaryView.tsx"
      to: "src/lib/route-visuals.ts"
      via: "imports addGradientRoute, addStartFinishMarkers"
      pattern: "from.*route-visuals"
    - from: "src/components/RunDetailOverlay.tsx"
      to: "src/lib/route-visuals.ts"
      via: "imports addGradientRoute, addStartFinishMarkers"
      pattern: "from.*route-visuals"
---

<objective>
Apply elevation gradient route rendering to RunSummaryView and RunDetailOverlay so the premium visual treatment is consistent across all map views.

Purpose: After completing a run or viewing run history, the route should display the same beautiful gradient coloring as the main MapView. These views already use dark-matter tiles — they just need the gradient route and markers.
Output: Updated RunSummaryView.tsx and RunDetailOverlay.tsx
</objective>

<execution_context>
@/Users/johannesstensson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johannesstensson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-route-visualization/07-RESEARCH.md
@.planning/phases/07-route-visualization/07-01-SUMMARY.md

<interfaces>
From src/lib/route-visuals.ts (Plan 01):
```typescript
export const DARK_STYLE: string;
export function addGradientRoute(map: maplibregl.Map, coords: [number, number][], grades: number[], sourceId?: string, layerId?: string): void;
export function addStartFinishMarkers(map: maplibregl.Map, coords: [number, number][]): maplibregl.Marker[];
export function removeRouteVisuals(map: maplibregl.Map): void;
```

From src/lib/elevation.ts (Plan 01):
```typescript
export async function fetchElevations(coords: [number, number][]): Promise<number[]>;
export function computeGrades(coords: [number, number][], elevations: number[]): number[];
```

From src/types/index.ts:
```typescript
export interface CompletedRun {
  id: string;
  startTime: number;
  endTime: number;
  elapsedMs: number;
  distanceMeters: number;
  trace: FilteredPosition[];
  routeId: string | null;
  routePolyline?: [number, number][]; // planned route at time of run
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade RunSummaryView planned route to gradient</name>
  <files>src/components/RunSummaryView.tsx</files>
  <read_first>
    - src/components/RunSummaryView.tsx (full current implementation)
    - src/lib/route-visuals.ts (Plan 01 output)
    - src/lib/elevation.ts (Plan 01 output)
  </read_first>
  <action>
Modify src/components/RunSummaryView.tsx:

1. IMPORTS: Add imports from elevation.ts (fetchElevations, computeGrades) and route-visuals.ts (addGradientRoute, addStartFinishMarkers).

2. PLANNED ROUTE LAYER: Replace the current planned-route source+layer (which uses a simple green line) with the gradient route approach:
   - After map 'load' event, if routePolyline exists:
   - Fetch elevation async: `fetchElevations(routePolyline).then(elevations => { const grades = computeGrades(routePolyline, elevations); addGradientRoute(map, routePolyline, grades, 'planned-route', 'planned-route-gradient'); }).catch(() => { addGradientRoute(map, routePolyline, [], 'planned-route', 'planned-route-gradient'); })`
   - Use line-width 3 for secondary rendering. Pass this via the grades — or better, after calling addGradientRoute, update the paint property: `map.setPaintProperty('planned-route-gradient', 'line-width', 3)`.
   - Add start/finish markers: `addStartFinishMarkers(map, routePolyline)`.

3. ACTUAL TRACE LAYER: Keep the existing actual-trace layer (red/blue line showing where the runner actually went). This stays as-is — it's the runner's GPS trace, not the planned route.

4. Z-ORDER: Ensure planned route gradient renders BELOW actual trace. Add planned route first, then actual trace on top.

5. Keep all existing functionality: stats display, save/discard buttons, fitBounds logic. Do NOT change component props.
  </action>
  <verify>
    <automated>cd /Users/johannesstensson/rundloop && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "addGradientRoute" src/components/RunSummaryView.tsx
    - grep -q "fetchElevations" src/components/RunSummaryView.tsx
    - grep -q "computeGrades" src/components/RunSummaryView.tsx
    - grep -q "addStartFinishMarkers" src/components/RunSummaryView.tsx
    - grep -q "route-visuals" src/components/RunSummaryView.tsx
    - grep -q "elevation" src/components/RunSummaryView.tsx
  </acceptance_criteria>
  <done>RunSummaryView renders planned route with elevation gradient (3px width). Start/finish markers present. Actual GPS trace still renders on top. Graceful fallback to solid green on elevation API failure. All existing functionality preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade RunDetailOverlay planned route to gradient</name>
  <files>src/components/RunDetailOverlay.tsx</files>
  <read_first>
    - src/components/RunDetailOverlay.tsx (full current implementation)
    - src/components/RunSummaryView.tsx (just updated — follow same pattern)
    - src/lib/route-visuals.ts (Plan 01 output)
  </read_first>
  <action>
Modify src/components/RunDetailOverlay.tsx with the same pattern as RunSummaryView:

1. IMPORTS: Add imports from elevation.ts (fetchElevations, computeGrades) and route-visuals.ts (addGradientRoute, addStartFinishMarkers).

2. PLANNED ROUTE LAYER: Replace the current planned-route source+layer with gradient approach:
   - Same async elevation fetch + gradient route pattern as RunSummaryView
   - Use sourceId 'planned-route', layerId 'planned-route-gradient'
   - Set line-width to 3 after adding gradient route
   - Add start/finish markers

3. ACTUAL TRACE: Keep the existing actual-trace layer as-is.

4. Z-ORDER: Planned route gradient below actual trace.

5. Keep all existing functionality: close button, stats, map bounds. Do NOT change component props.
  </action>
  <verify>
    <automated>cd /Users/johannesstensson/rundloop && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "addGradientRoute" src/components/RunDetailOverlay.tsx
    - grep -q "fetchElevations" src/components/RunDetailOverlay.tsx
    - grep -q "computeGrades" src/components/RunDetailOverlay.tsx
    - grep -q "addStartFinishMarkers" src/components/RunDetailOverlay.tsx
    - grep -q "route-visuals" src/components/RunDetailOverlay.tsx
  </acceptance_criteria>
  <done>RunDetailOverlay renders planned route with elevation gradient (3px width). Start/finish markers present. Actual GPS trace still renders on top. Graceful fallback on elevation API failure. All existing functionality preserved.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no new errors
- `npx vitest run --reporter=verbose` all existing tests still pass
- Both components import from route-visuals.ts and elevation.ts
- Both components still use dark-matter tiles (already had them)
</verification>

<success_criteria>
- RunSummaryView shows gradient planned route with markers
- RunDetailOverlay shows gradient planned route with markers
- Both use 3px line width for secondary route display
- Both fall back to solid green on elevation API failure
- Actual GPS trace renders above planned route in both views
- No regression in existing component functionality
</success_criteria>

<output>
After completion, create `.planning/phases/07-route-visualization/07-03-SUMMARY.md`
</output>
