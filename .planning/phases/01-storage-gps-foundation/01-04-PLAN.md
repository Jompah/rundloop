---
phase: 01-storage-gps-foundation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
autonomous: true
gap_closure: true
requirements:
  - GPS-01
  - STOR-03

must_haves:
  truths:
    - "initDB() is called on app mount so migration and storage persistence fire at runtime"
    - "watchFilteredPosition is documented as a hard Phase 2 dependency (no app wiring in Phase 1)"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "initDB() call on mount"
      contains: "initDB"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/lib/db.ts"
      via: "import and useEffect call"
      pattern: "import.*initDB.*from.*db"
---

<objective>
Close two verification gaps from Phase 1: (1) wire initDB() into the app entry point so IndexedDB migration and storage persistence actually fire at runtime, and (2) document the GPS filter consumer wiring as a hard Phase 2 dependency.

Purpose: STOR-03 (storage persistence) is blocked because initDB() is never called. GPS-01 filter exists but is not consumed -- this is by design for Phase 1 but must be explicitly noted for Phase 2.
Output: Updated page.tsx with initDB() call; GPS filter gap documented for Phase 2 planning.
</objective>

<execution_context>
@/Users/johannesstensson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johannesstensson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-storage-gps-foundation/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire initDB() call into page.tsx on app mount</name>
  <files>src/app/page.tsx</files>
  <read_first>src/app/page.tsx, src/lib/db.ts</read_first>
  <action>
In src/app/page.tsx:
1. Add import: `import { initDB } from '@/lib/db';`
2. Add a new useEffect at the top of the component body (before the existing useEffect that sets up GPS watching):
```typescript
useEffect(() => {
  initDB();
}, []);
```
This calls initDB() once on mount. initDB() is async and handles its own errors internally (logs warnings for persistence failures). Fire-and-forget is correct here -- no await needed because:
- The lazy getDB() singleton ensures any subsequent db operation waits for the connection
- initDB() triggers migrateFromLocalStorage() and requestPersistentStorage() as side effects
- These are best-effort operations that should not block app rendering

Do NOT modify any other logic in page.tsx. Do NOT change the existing useEffect. Only add the import and the new useEffect.
  </action>
  <verify>
    <automated>grep -n "initDB" src/app/page.tsx | head -5</automated>
  </verify>
  <acceptance_criteria>
- grep "import.*initDB.*from.*db" src/app/page.tsx returns a match
- grep "initDB()" src/app/page.tsx returns a match inside a useEffect
- npx tsc --noEmit 2>&amp;1 | grep "page.tsx" returns no NEW errors (existing async consumer errors from Phase 1 are expected)
  </acceptance_criteria>
  <done>page.tsx imports initDB from @/lib/db and calls it in a useEffect([], []) on mount. Storage persistence (STOR-03) now fires at runtime.</done>
</task>

<task type="auto">
  <name>Task 2: Document GPS filter wiring as Phase 2 hard dependency</name>
  <files>src/app/page.tsx</files>
  <read_first>src/app/page.tsx, src/lib/geolocation.ts</read_first>
  <action>
In src/app/page.tsx, add a code comment near the existing useEffect that calls watchPosition (the GPS setup useEffect). Add this comment directly above the watchPosition call:

```typescript
// TODO(Phase-2): Replace watchPosition with watchFilteredPosition from '@/lib/geolocation'
// to activate GPS accuracy/teleport/jitter filtering (GPS-01). Filter is implemented and
// tested in gps-filter.ts but intentionally not wired until the run session lifecycle exists.
```

This is a documentation-only change. Do NOT change watchPosition to watchFilteredPosition -- that requires the Phase 2 run session state machine to work correctly.
  </action>
  <verify>
    <automated>grep -n "TODO(Phase-2).*watchFilteredPosition" src/app/page.tsx | head -3</automated>
  </verify>
  <acceptance_criteria>
- grep "TODO(Phase-2).*watchFilteredPosition" src/app/page.tsx returns a match
- No functional code changes beyond the comment
  </acceptance_criteria>
  <done>GPS filter consumer gap is documented as a hard Phase 2 dependency with a TODO comment referencing GPS-01 and watchFilteredPosition.</done>
</task>

</tasks>

<verification>
1. `grep "initDB" src/app/page.tsx` shows import and useEffect call
2. `grep "TODO(Phase-2).*watchFilteredPosition" src/app/page.tsx` shows documentation comment
3. `npx next build 2>&1 | tail -5` completes without new build errors
</verification>

<success_criteria>
- initDB() is imported and called on mount in page.tsx (STOR-03 unblocked)
- GPS filter wiring documented as Phase 2 TODO (GPS-01 gap acknowledged)
- No regressions in existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/01-storage-gps-foundation/01-04-SUMMARY.md`
</output>
